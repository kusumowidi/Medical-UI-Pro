"""
tests/__init__.py — Test package marker for Medical UI Pro test suite.

All tests in this package are designed to run headless (no display, no GPU,
no camera). They cover: STL loading, mesh normalization, software renderer
math, and gesture classification logic.

Run with:
    python -m pytest tests/ -v
"""
